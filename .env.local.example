# Copy to .env.local (already gitignored) and fill in.
# Only used by `npm run sync-scores` — never read by the Next.js app itself.
# Must be an account you've added to the `admins` Cognito group (see README).
SYNC_ADMIN_EMAIL=
SYNC_ADMIN_PASSWORD=
